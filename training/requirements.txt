# Training deps (run on a GPU box). Install the CUDA torch wheel separately,
# e.g. pip install torch --index-url https://download.pytorch.org/whl/cu124
transformers>=4.41,<5
datasets>=2.19
pillow>=10
numpy>=1.26
scikit-learn>=1.4
accelerate>=0.30
